$("#project_info-<%= @project_info.file_type_id %>").html("<%= j(render partial: 'project_info_list',locals: {file_type_children_id: @project_info.file_type_id })%>");
$(".ui-dialog-button button[i-id=ok]").attr("disabled",false);
var d = dialog.get("add_project_info");
d.close().remove();